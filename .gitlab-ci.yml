stages:
  - build
  - deploy

build test-frontend:
  stage: build
  image: node:12-alpine
  cache:
    paths:
      - node_modules
  artifacts:
    paths:
      - dist
  tags:
    - frontlatest
  script:
    - npm install
    - npm run build

deploy test-frontend:
  stage: deploy
  only:
    refs:
      - main
  tags:
    - frontlatest
  script:
    - mkdir -p ~/frontend
    - rm -rf ~/frontend/*
    - cp -r dist/0302-frontend/. ~/frontend